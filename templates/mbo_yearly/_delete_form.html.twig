<form method="post" action="{{ path('mbo_yearly_delete', {'id': mbo_yearly.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ mbo_yearly.id) }}">
    <button class="btn">Delete</button>
</form>
