{% extends 'base.html.twig' %}

{% block title %}MBOYearly{% endblock %}

{% block body %}
    <h1>MBOYearly</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ m_b_o_yearly.id }}</td>
            </tr>
            <tr>
                <th>Year</th>
                <td>{{ m_b_o_yearly.year }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ m_b_o_yearly.Type }}</td>
            </tr>
            <tr>
                <th>Subject</th>
                <td>{{ m_b_o_yearly.subject }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ m_b_o_yearly.description }}</td>
            </tr>
            <tr>
                <th>Weight</th>
                <td>{{ m_b_o_yearly.weight }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ m_b_o_yearly.status }}</td>
            </tr>
            <tr>
                <th>Score</th>
                <td>{{ m_b_o_yearly.score }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('m_b_o_yearly_index') }}">back to list</a>

    <a href="{{ path('m_b_o_yearly_edit', {'id': m_b_o_yearly.id}) }}">edit</a>

    {{ include('m_b_o_yearly/_delete_form.html.twig') }}
{% endblock %}