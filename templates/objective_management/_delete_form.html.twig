<form method="post" action="{{ path('objective_management_delete', {'id': objective_management.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objective_management.id) }}">
    <button class="btn">Delete</button>
</form>