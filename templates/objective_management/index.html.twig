{% extends 'base.html.twig' %}

{% block title %}ObjectiveManagement index{% endblock %}

{% block body %}
    <h1>ObjectiveManagement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for objective_management in objective_managements %}
            <tr>
                <td>{{ objective_management.id }}</td>
                <td>
                    <a href="{{ path('objective_management_show', {'id': objective_management.id}) }}">show</a>
                    <a href="{{ path('objective_management_edit', {'id': objective_management.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('objective_management_new') }}">Create new</a>
{% endblock %}